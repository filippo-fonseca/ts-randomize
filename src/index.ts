/**
 * An *incredibly* complex util function that returns the value of a random index (from a provided array).
 */

import { getRandomValue } from "./random";

export function random(data: any[]) {
    const run = getRandomValue(data);

    return run;
}






































