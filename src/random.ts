export function getRandomValue(data: any[]) {
    const randomGen: number = Math.floor(Math.random() * data.length);
    const randomIndex: any = data[randomGen];

    return randomIndex;
}






































